import styled from 'styled-components';
import { Box } from 'rebass';

export const ContainerSt = styled(Box)`
  text-align: center;
  max-width: 500px;
  margin: auto;
`;

export const AnimationSt = styled.div``;
